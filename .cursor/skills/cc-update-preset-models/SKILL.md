---
name: cc-update-preset-models
description: >
  Stub: load the canonical skill at skills/cc-update-preset-models/.
  Trigger: /cc-update-preset-models, /cc:update-preset-models, refresh
  src/presets/models agent slugs. Not part of installable presets.
license: Apache-2.0
metadata:
  author: lgzarturo
  version: "1.0"
---

# cc-update-preset-models (Cursor stub)

**Canonical skill:**
[`skills/cc-update-preset-models/SKILL.md`](../../../skills/cc-update-preset-models/SKILL.md)

1. Read that file completely and [references/role-map.md](../../../skills/cc-update-preset-models/references/role-map.md).
2. Follow its workflow (live catalogs → YAML → tests → verify).
3. Do **not** copy this skill into `presets/`.

If `install preset --target cursor` overwrote this stub, restore commands from
`skills/cc-update-preset-models/assets/commands/`.
