# `cc setup`

`setup` detects the project, initializes CodeConductor, installs the selected
target preset, and runs `doctor`.

```bash
cc setup --target claude --locale es --yes
cc setup --dry-run
```
