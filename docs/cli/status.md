# `cc status`

`status` is an installation dashboard. It reports target presence,
configuration, update/conflict state, and locally modified managed files
without performing the deeper diagnostic work of `doctor`.
