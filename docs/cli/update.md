# `cc update`

Use `cc update --check` to distinguish the installed CLI from npm latest, then
run the latest executable to reconcile the project harness. See
[updating](../getting-started/updating.md) for safety behavior.
