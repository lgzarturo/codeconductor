# Versioning

`cc --version` identifies the executable. `cc version --json` additionally
reports the project's harness version, configuration-state schema, installed
targets, and tracked skills. These versions intentionally evolve separately.
