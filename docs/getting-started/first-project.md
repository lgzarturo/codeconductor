# First project

The minimum operational loop is:

```bash
cc status
cc doctor
cc update --check
```

`status` shows what is installed, while `doctor` diagnoses configuration and
generated files. Use `cc help` to discover workflows after the harness is
installed.
