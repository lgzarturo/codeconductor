# Delta Spec: Router /cc:ask

## ADDED Requirements

- `/cc:ask` / CLI `ask` maps natural language to exactly one catalog slash command
- The recommendation includes a justification string
- Catalog ids exist as `presets/claude/commands/cc/<command>.md`
- The recommended workflow is not started (`executed: false`)
