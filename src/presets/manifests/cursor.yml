target: cursor
entries:
  - src: opencode/agents
    dest: .cursor/agents
    strategy: overwrite
    template: true
  - src: opencode/prompts/v0.4.0
    dest: .cursor/prompts/v0.4.0
    strategy: overwrite
    template: true
